# Human Approval

RUNA.md #4, [docs/approval.md](../../docs/approval.md).

* **`needs_approval.py`** -- gating a tool with `needs_approval`, and resolving the pause.
* **`durable_resume.py`** -- serializing a paused `RunState` and resuming it as a fresh process would.
