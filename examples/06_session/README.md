# Session

RUNA.md #6, [docs/sessions.md](../../docs/sessions.md).

* **`sqlite_session.py`** -- persisting history to `runa.db` with `SQLiteSession`.
* **`custom_session_store.py`** -- `SessionABC`'s four abstract methods, nothing less.
