# Quickstart

The smallest useful Runa agent: one `Agent`, one `@tool`, one call.

* **`hello.py`** -- a greeter agent that can tell the time.

Every other folder here covers one more primitive from [RUNA.md](../../RUNA.md), in the same
numbered order.
