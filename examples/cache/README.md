# Cache

[docs/cache.md](../../docs/cache.md).

* **`basic_cache.py`** -- `MemoryCache` and `SQLiteCache`, the same get/set/delete/clear interface.

Not one of RUNA.md's 14 numbered primitives (hence no number prefix), but documented and used
the same way, so it gets an example too.
