# Tracing

RUNA.md #14, [docs/tracing.md](../../docs/tracing.md).

* **`inspect_trace.py`** -- `run.trace`'s span tree, recorded automatically for every run.
* **`custom_exporter.py`** -- `add_exporter`, adding a `TraceExporter` alongside the default `SQLiteExporter`.
