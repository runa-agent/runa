# Hooks

RUNA.md #11, [docs/tracing.md](../../docs/tracing.md#hooks).

* **`run_hooks.py`** -- `RunHooks`, passed per-call, firing for every agent in the run.
* **`agent_hooks.py`** -- `AgentHooks`, scoped to one `Agent` subclass.
