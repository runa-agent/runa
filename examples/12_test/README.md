# Test

RUNA.md #12, [docs/evaluation.md](../../docs/evaluation.md).

* **`test_example.py`** -- a bare `test_*` function with a plain `assert`, no pytest.

A real app's tests live under `tests/`, run with `runa test`.
