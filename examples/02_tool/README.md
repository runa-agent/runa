# Tool

RUNA.md #2, [docs/tools.md](../../docs/tools.md).

* **`basic_tool.py`** -- a plain function wrapped in `@tool`.
* **`async_tool.py`** -- an `async def` tool, and the reserved `ctx` parameter.
