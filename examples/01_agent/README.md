# Agent

RUNA.md #1, [docs/agents.md](../../docs/agents.md).

* **`basic_agent.py`** -- the `Agent` subclass, running it, and reading back `Run`.
* **`dynamic_instructions.py`** -- `instructions` as a `(context) -> str` function.
