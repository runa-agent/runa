# Model

RUNA.md #10, [docs/models.md](../../docs/models.md).

* **`multi_provider.py`** -- mixing providers across agents: a cheap router, a stronger answerer.
* **`model_settings.py`** -- tuning sampling with `ModelSettings`.
