# Eval (Case/Dataset)

RUNA.md #13, [docs/evaluation.md](../../docs/evaluation.md).

* **`case_dataset.py`** -- module-level `agent` and `dataset`, graded with `agent.evaluate(...)`.

A real app's datasets live under `evals/`, run with `runa eval`.
